import MenuSection from "./MenuSection";

export default {
  title: "MenuSection",
  component: MenuSection,
};

export const ExampleMenuSection = () => {
  return (
    <MenuSection title="Filters">
      <p>test</p>
    </MenuSection>
  );
};
